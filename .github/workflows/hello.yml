name: hello

on:
  push:
    branches: [main]

jobs:
   greet:
     runs-on: ubuntu-latest
       name: say hello to users
       run: echo "Hello Dosto"

       name: say hello to logged in user
       run: echo "Hello $USER"
