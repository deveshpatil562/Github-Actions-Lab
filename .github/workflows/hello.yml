name: hello

on:
  push:
    branches: [main]

jobs:
   greet:
     runs-on: ubuntu-latest
    steps:     
      name: say hello to users
      run: echo "Hello Dosto"
    steps:
      name: say hello to logged in user
      run: echo "Hello $USER"
