name: scheduled Workflow

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  daily-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Schedule run
        run: echo "Scheduled workflow running at midnight that is 00:00"
