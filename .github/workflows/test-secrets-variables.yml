name: Secret Test

on:
  workflow_dispatch:

jobs:
  check_secret:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret key existance
        run: 'echo "The secret is set: true"'

      - name: Printing secret key directly
        run: 'echo "Secret value: ${{ secrets.MY_SECRET_MESSAGE }}"'